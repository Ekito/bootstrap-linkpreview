$(function() {
    $("a").linkpreview({
        previewContainer: "#preview-container"
    });
    $("input").linkpreview({
        previewContainer: "#preview-container2",
        refreshButton: "#refresh-button"
    });
});